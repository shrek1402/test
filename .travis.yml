image: gcc
language: cpp

         - MATRIX_EVAL="CC=gcc-7 && CXX=g++-7"

script:
    - sudo apt-get install libncursesw5-dev
    - make build
    - make test
    - cd bin/
    - ls
    - ./Keyboard-Ninja-test
